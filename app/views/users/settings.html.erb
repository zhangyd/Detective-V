<p id="notice"><%= notice %></p>

<!-- <form>
  <div class="form-group">
    <label for="accessTokenInput">Input Access Token</label>
    <input type="text" class="form-control" id="accessTokenInput" aria-describedby="accessTokenHelp" placeholder="40 character token here">
    <small id="accessTokenHelp" class="form-text text-muted">You only need to do this once!</small>
  </div>
  <button type="submit" class="btn btn-default">Submit</button>
</form> -->

<% simple_form_for(@user, url: user_settings_path(@user), html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :access_token, placeholder: '40 character token here' %>
  <%= f.button :submit %>
<% end %>
	